#entry main

:main
	ldw %ax &last		; Print `large`
	ldw %bx &large
	ldw %cx &more
	ldb %dx &test
	sysi
	
	mov %ax $3			; Print new-line
	mov %bx $10
	sysi
	
	mov %ax $1			; Exit
	mov %bx $0
	sysi
	
#data
	stw *large $5124
	stw *more $300
	stb *test $43
	stw *last $4
#end