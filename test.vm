:start	
	mov %ax $1			; Set our system interrupt
	mov %bx $6			; Set our number to square
	call @square		; Call our function
	sysi				; System interrupt

:square
	mul %bx %bx			; Multiply the number by itself
	ret					; Return